# terraform-script-to-import-the-exisiting-virtual-machine

This is an example of terraform import, where it iports the existing VM on GCP, which imports the config of the vm machine into the tf file
